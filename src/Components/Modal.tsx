import './arquivo.css'

interface CategoriasProviderProps {
    children: React.ReactNode;
}

const Modal: React.FC<CategoriasProviderProps> = () => {
    return (
        <div className="modal_style">
        
        </div>
    );
}

export default Modal